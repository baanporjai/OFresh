# ข้อมูลลับ ห้ามหลุด
gsheet_creds.json

# ไฟล์ข้อมูล/สำรองข้อมูลธุรกิจ ไม่ใช่โค้ดเว็บ
backup/
source_nayax/
*.xlsx
~$*.xlsx
update_nayax.log
*.log

# ไฟล์ระบบอื่นๆ ที่ไม่เกี่ยวกับเว็บ
*.apk
*.apk.*
*.pdf
เปลี่ยนม้วนฟืลม์

# โฟลเดอร์ย่อยที่มี .git ของตัวเอง (repo ซ้อน repo) — จัดการแยกต่างหาก ไม่ track ในนี้
/OFresh/
/LINE OA Chatbot/line-bot-ai/

# ไฟล์ต้นฉบับงานออกแบบขนาดใหญ่ที่เว็บไม่ได้เรียกใช้จริง (วิดีโอ/ไฟล์ต้นฉบับ .ai/.pptx)
Design/*.mp4
Design/*.ai
Design/*.pptx

# dependency / build cache ของโปรเจกต์ย่อย
node_modules/
.wrangler/

# ไฟล์ระบบของแต่ละเครื่อง
.DS_Store
Thumbs.db
