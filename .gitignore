# ข้อมูลลับ ห้ามหลุด
gsheet_creds.json

# ไฟล์/โฟลเดอร์ข้อมูลธุรกิจ (ยอดขาย, คู่มือ, เอกสารเครื่อง) ไม่ใช่โค้ดเว็บ — ดูโฟลเดอร์นี้แยกไว้แล้ว
/business-data/

# กันไว้เผื่อมีไฟล์ประเภทนี้หลุดมาที่ root อีกในอนาคต
*.xlsx
~$*.xlsx
*.log
*.pdf
*.apk
*.apk.*

# โฟลเดอร์ย่อยที่มี .git ของตัวเอง (repo ซ้อน repo) — จัดการแยกต่างหาก ไม่ track ในนี้
/LINE OA Chatbot/line-bot-ai/

# ไฟล์ต้นฉบับงานออกแบบขนาดใหญ่ที่เว็บไม่ได้เรียกใช้จริง (วิดีโอ/ไฟล์ต้นฉบับ .ai/.pptx)
Design/*.mp4
Design/*.ai
Design/*.pptx

# dependency / build cache ของโปรเจกต์ย่อย
node_modules/
.wrangler/

# ไฟล์ระบบของแต่ละเครื่อง
.DS_Store
Thumbs.db
